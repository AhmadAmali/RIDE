---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - .env.example
  - .gitignore
  - Makefile
  - pyproject.toml
  - .pre-commit-config.yaml
  - backend/Dockerfile
  - backend/requirements.txt
  - backend/ride/__init__.py
  - backend/ride/config.py
  - backend/ride/db/__init__.py
  - backend/ride/db/session.py
  - backend/ride/api/__init__.py
  - backend/ride/api/main.py
  - backend/ride/api/routes/__init__.py
  - backend/ride/models/__init__.py
  - backend/ride/workers/__init__.py
  - backend/ride/kafka/__init__.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "docker compose up starts Kafka (KRaft), PostgreSQL, Qdrant, and the backend -- all containers healthy"
    - "FastAPI health check at GET /health responds with 200 and JSON body"
    - "CORS is configured for http://localhost:3000 with credentials support"
    - "Makefile commands (make up, make down, make logs) work correctly"
    - "Ruff linter passes on all Python files"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Service orchestration for Kafka KRaft, PostgreSQL, Qdrant, backend"
      contains: "service_healthy"
    - path: "backend/ride/config.py"
      provides: "Pydantic Settings with database_url, kafka_bootstrap_servers, cors_origins"
      exports: ["Settings", "get_settings", "settings"]
    - path: "backend/ride/db/session.py"
      provides: "Async SQLAlchemy engine and session factory"
      exports: ["engine", "async_session_maker", "Base", "get_db"]
    - path: "backend/ride/api/main.py"
      provides: "FastAPI app with lifespan, CORS, health endpoint"
      exports: ["app"]
    - path: "backend/Dockerfile"
      provides: "Python container for backend service"
    - path: "Makefile"
      provides: "Developer workflow commands"
  key_links:
    - from: "docker-compose.yml"
      to: ".env.example"
      via: "env_file and environment variable defaults"
      pattern: "env_file.*\\.env"
    - from: "backend/ride/api/main.py"
      to: "backend/ride/config.py"
      via: "settings import for CORS origins"
      pattern: "from ride\\.config import settings"
    - from: "backend/ride/api/main.py"
      to: "backend/ride/db/session.py"
      via: "engine.dispose() in lifespan shutdown"
      pattern: "from ride\\.db\\.session import engine"
    - from: "docker-compose.yml"
      to: "backend/Dockerfile"
      via: "build context for backend service"
      pattern: "build.*\\./backend"
---

<objective>
Create the runnable infrastructure skeleton: Docker Compose orchestrating Kafka (KRaft, no Zookeeper), PostgreSQL, Qdrant, and the FastAPI backend with health checks, plus all project scaffolding (directory structure, config, dev tooling).

Purpose: Establish the foundation that all downstream pipeline work builds on. After this plan, `docker compose up` produces four healthy containers and `curl localhost:8000/health` returns 200.

Output: Working Docker Compose stack, FastAPI app with health endpoint, project directory structure, Makefile, dev tooling (ruff, pre-commit).
</objective>

<execution_context>
@/home/ahmadamali2/.claude/get-shit-done/workflows/execute-plan.md
@/home/ahmadamali2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project scaffold, Docker Compose, and dev tooling</name>
  <files>
    docker-compose.yml,
    .env.example,
    .gitignore,
    Makefile,
    pyproject.toml,
    .pre-commit-config.yaml,
    backend/Dockerfile,
    backend/requirements.txt,
    backend/ride/__init__.py,
    backend/ride/config.py,
    backend/ride/db/__init__.py,
    backend/ride/db/session.py,
    backend/ride/models/__init__.py,
    backend/ride/workers/__init__.py,
    backend/ride/kafka/__init__.py,
    backend/ride/api/__init__.py,
    backend/ride/api/routes/__init__.py
  </files>
  <action>
    Create the full project directory structure per CONTEXT.md and RESEARCH.md layout.

    **docker-compose.yml:** Define 4 services:
    - `kafka`: bitnami/kafka:latest with KRaft config (no Zookeeper). Environment vars: KAFKA_CFG_NODE_ID=0, KAFKA_CFG_PROCESS_ROLES=controller,broker, listeners on 9092 (internal PLAINTEXT), 9093 (CONTROLLER), 9094 (EXTERNAL). KAFKA_CFG_ADVERTISED_LISTENERS=PLAINTEXT://kafka:9092,EXTERNAL://localhost:9094. KAFKA_CFG_CONTROLLER_QUORUM_VOTERS=0@kafka:9093. Replication factors all set to 1. Health check: `kafka-broker-api-versions.sh --bootstrap-server kafka:9092 || exit 1` with interval=15s, timeout=10s, retries=10, start_period=30s. Volume: kafka_data:/bitnami/kafka.
    - `postgres`: postgres:16-alpine. Environment reads from .env with defaults (POSTGRES_USER=ride, POSTGRES_PASSWORD=ride, POSTGRES_DB=ride). Health check: `pg_isready -U ${POSTGRES_USER:-ride}` with interval=10s, timeout=5s, retries=5. Volume: postgres_data. Port: 5432:5432.
    - `qdrant`: qdrant/qdrant:latest. Ports 6333:6333 and 6334:6334. Volume: qdrant_data:/qdrant/storage. Health check: use the `/dev/tcp` bash workaround from RESEARCH.md (bash -c 'exec 3<>/dev/tcp/127.0.0.1/6333 ...') with interval=10s, timeout=5s, retries=10, start_period=15s.
    - `backend`: build from ./backend. Port 8000:8000. env_file: .env. Volume mount ./backend:/app for hot-reload. Command: `uvicorn ride.api.main:app --host 0.0.0.0 --port 8000 --reload`. depends_on all three infra services with condition: service_healthy.
    - Declare named volumes: kafka_data, postgres_data, qdrant_data.

    **.env.example:** Template with all config vars:
    ```
    # Database
    DATABASE_URL=postgresql+asyncpg://ride:ride@postgres:5432/ride
    POSTGRES_USER=ride
    POSTGRES_PASSWORD=ride
    POSTGRES_DB=ride

    # Kafka
    KAFKA_BOOTSTRAP_SERVERS=kafka:9092

    # API
    CORS_ORIGINS=["http://localhost:3000"]
    API_HOST=0.0.0.0
    API_PORT=8000
    ```

    **.gitignore:** Standard Python gitignore plus: .env, __pycache__, *.pyc, .ruff_cache, alembic/versions/*.pyc, .mypy_cache, .pytest_cache, *.egg-info, dist/, build/, volumes/.

    **backend/Dockerfile:** Python 3.11-slim base. WORKDIR /app. COPY requirements.txt and pip install. COPY ride/ package. EXPOSE 8000. CMD uvicorn.

    **backend/requirements.txt:** Pin versions per RESEARCH.md:
    ```
    fastapi>=0.115.0,<1.0.0
    uvicorn[standard]>=0.30.0,<1.0.0
    sqlalchemy[asyncio]>=2.0.0,<3.0.0
    asyncpg>=0.28.0,<0.30.0
    alembic>=1.13.0,<2.0.0
    pydantic-settings>=2.0.0,<3.0.0
    python-dotenv>=1.0.0,<2.0.0
    aiokafka>=0.11.0,<1.0.0
    httpx>=0.27.0,<1.0.0
    ruff>=0.4.0
    pre-commit>=3.0.0
    ```

    **backend/ride/config.py:** Pydantic Settings class per RESEARCH.md Pattern 4. Fields: database_url (str, default postgresql+asyncpg://ride:ride@localhost:5432/ride), kafka_bootstrap_servers (str, default localhost:9092), cors_origins (list[str], default ["http://localhost:3000"]), api_host (str, default 0.0.0.0), api_port (int, default 8000). Use SettingsConfigDict with env_file=".env". Export get_settings with @lru_cache and module-level `settings = get_settings()`.

    **backend/ride/db/session.py:** Per RESEARCH.md Pattern 3. create_async_engine with settings.database_url, pool_pre_ping=True. async_sessionmaker with expire_on_commit=False. DeclarativeBase class named Base. Async generator get_db that yields AsyncSession.

    **Makefile:** Targets: up (docker compose up -d), down (docker compose down), logs (docker compose logs -f backend), migrate (docker compose exec backend alembic upgrade head), shell (docker compose exec backend python), topics (docker compose exec backend python -m ride.kafka.create_topics). Use .PHONY declarations.

    **pyproject.toml:** Ruff config per RESEARCH.md — line-length=100, target-version="py311", select=["E","F","UP","I","B"], extend-immutable-calls for FastAPI Depends and Query.

    **.pre-commit-config.yaml:** Per RESEARCH.md — ruff-pre-commit repo with ruff (--fix) and ruff-format hooks.

    All __init__.py files: Empty files to make Python packages.
  </action>
  <verify>
    <automated>cd /home/ahmadamali2/RIDE && python -c "
import sys; sys.path.insert(0, 'backend');
from ride.config import settings;
assert settings.database_url.startswith('postgresql+asyncpg://'), 'Bad DB URL';
assert settings.kafka_bootstrap_servers, 'Missing Kafka servers';
assert 'http://localhost:3000' in settings.cors_origins, 'Missing CORS origin';
from ride.db.session import Base, engine, async_session_maker, get_db;
assert Base is not None;
print('All imports and assertions pass')
"</automated>
  </verify>
  <done>
    - All directories and files from the project structure exist
    - config.py imports successfully with correct defaults
    - db/session.py imports successfully with Base, engine, async_session_maker, get_db
    - docker-compose.yml defines 4 services with health checks
    - .env.example has all environment variables documented
    - Makefile has up/down/logs/migrate/shell/topics targets
    - pyproject.toml has ruff configuration
    - .pre-commit-config.yaml has ruff hooks
  </done>
</task>

<task type="auto">
  <name>Task 2: Create FastAPI application with health endpoint and CORS</name>
  <files>
    backend/ride/api/main.py,
    backend/ride/api/routes/__init__.py
  </files>
  <action>
    Create the FastAPI application per RESEARCH.md Pattern 4.

    **backend/ride/api/main.py:**
    - Import asynccontextmanager from contextlib
    - Import FastAPI, CORSMiddleware
    - Import settings from ride.config
    - Import engine from ride.db.session
    - Create lifespan async context manager: yield on startup, engine.dispose() on shutdown
    - Create FastAPI app with lifespan=lifespan, title="RIDE API", version="0.1.0"
    - Add CORSMiddleware FIRST: allow_origins=settings.cors_origins, allow_credentials=True, allow_methods=["*"], allow_headers=["*"]. Do NOT use allow_origins=["*"] with allow_credentials=True per CORS spec warning in RESEARCH.md.
    - Add GET /health endpoint returning {"status": "ok", "service": "ride-api"}

    **backend/ride/api/routes/__init__.py:** Empty for now -- routes will be added in Phase 2.

    After creating the files, verify that ruff passes on all backend Python files:
    ```bash
    cd /home/ahmadamali2/RIDE && pip install ruff >/dev/null 2>&1 && ruff check backend/
    ```
  </action>
  <verify>
    <automated>cd /home/ahmadamali2/RIDE && python -c "
import sys; sys.path.insert(0, 'backend');
from ride.api.main import app;
assert app.title == 'RIDE API';
routes = [r.path for r in app.routes];
assert '/health' in routes, f'Missing /health route, found: {routes}';
print('FastAPI app verified: title OK, /health route present')
" && ruff check backend/</automated>
  </verify>
  <done>
    - FastAPI app imports successfully
    - /health endpoint is registered and would return {"status": "ok", "service": "ride-api"}
    - CORS middleware configured with settings.cors_origins (not wildcard)
    - Lifespan context manager disposes engine on shutdown
    - All Python files pass ruff linting
  </done>
</task>

</tasks>

<verification>
1. All backend Python files import without errors: `python -c "import sys; sys.path.insert(0,'backend'); from ride.api.main import app; from ride.config import settings; from ride.db.session import Base"`
2. Ruff passes: `ruff check backend/`
3. docker-compose.yml is valid YAML: `python -c "import yaml; yaml.safe_load(open('docker-compose.yml'))"`
4. All expected directories exist under backend/ride/
</verification>

<success_criteria>
- Project directory structure matches CONTEXT.md layout (backend/ride/ with api/, workers/, models/, kafka/, db/ submodules)
- docker-compose.yml defines Kafka KRaft, PostgreSQL, Qdrant, and backend with health checks and depends_on conditions
- FastAPI app with /health endpoint, CORS, and lifespan imports and passes static analysis
- Config reads from .env via Pydantic Settings with correct defaults
- DB session factory uses async engine with asyncpg
- Makefile, .env.example, .gitignore, pyproject.toml, .pre-commit-config.yaml all present
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
